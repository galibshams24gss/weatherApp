export const https = (state) => {
    return state.https
}

export const query = (state) => {
    return state.query
}

export const api_key = (state) => {
    return state.api_key
}

export const cnt = (state) => {
    return state.cnt
}

export const forecastsData = (state) => {
    return state.forecastsData
}

export const currentWeather = (state) => {
    return state.currentWeather
}